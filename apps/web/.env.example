# Copiar a apps/web/.env (gitignorado) para desarrollo local (`npm run dev -w apps/web`).
# Vite carga .env en modo "development" — no afecta el build de producción, que usa
# .env.production (commiteado, ver ese archivo).
VITE_API_URL=http://localhost:3000
